{
    "configurations": [
        {
            "name": "Unity Editor",
            "type": "unity",
            "path": "${workspaceFolder}/Library/EditorInstance.json",
            "request": "launch"
        }
    ]
}
